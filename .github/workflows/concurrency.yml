name: Concurrency Testing

on:
  workflow_dispatch:


concurrency:
  group: concurrency-testing
  cancel-in-progress: true

jobs:
  test-timeout:
    runs-on: ubuntu-latest
    steps:
      - name: Some Step
        run: sleep 50
        timeout-minutes: 1


  #group:  «потрібно передати сюди якесь айді, назву, гілку і дт.
  #cancel-in-progress:  true // це буде відміняти всі попередні і запускати тільки останній.  Якщо не вказувати,
  #  то буде той що зараз і останній  в черзі.