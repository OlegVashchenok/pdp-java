name: reuse action

on: [push]

jobs:
  run_first_step:
    runs-on: ubuntu-latest
    steps:
      - name: Reuse Java Version Action
        uses: ./.github/actions/java_version
        with:
          java-version: 21
